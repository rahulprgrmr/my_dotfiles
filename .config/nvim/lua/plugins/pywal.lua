return {
	-- "nvim-lua/plenary.nvim", -- dummy plugin just for loading order
	-- name = "pywal-colors",
	-- enabled = false,
	-- lazy = false,
	-- priority = 1000, -- high priority to load before others
	-- config = function()
	-- 	local wal_file = vim.fn.expand("~/.cache/wal/colors-wal.vim")
	-- 	if vim.fn.filereadable(wal_file) == 1 then
	-- 		vim.cmd("source " .. wal_file)
	-- 	else
	-- 		vim.notify("Pywal color file not found: " .. wal_file, vim.log.levels.WARN)
	-- 	end
	-- end,
}
